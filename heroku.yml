setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
    - plan: heroku-redis
      as: REDIS

build:
  docker:
    client: client/Dockerfile
    api: server/Dockerfile
  config:
    NODE_ENV: production