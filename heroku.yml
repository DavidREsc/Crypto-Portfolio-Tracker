setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
    - plan: heroku-redis
      as: REDIS

build:
  docker:
    client: client/Dockerfile
    api: server/Dockerfile
  config:
    NODE_ENV: production

run:
  api: node server/index.js --bind 0.0.0.0:$PORT